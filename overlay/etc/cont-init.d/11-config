#!/usr/bin/with-contenv sh

set -euo pipefail
export BF_E=`basename ${0}`


#======================================================================================================================
# Create configuration files.
#======================================================================================================================

bf-echo "Writing clamd config..."
esh -o ${CLAMAV_CLAMD_CONF} \
    ${BF_TEMPLATES}/clamd.conf.esh
bf-done

bf-echo "Writing freshclam config..."
esh -o ${CLAMAV_FRESHCLAM_CONF} \
    ${BF_TEMPLATES}/freshclam.conf.esh
bf-done
